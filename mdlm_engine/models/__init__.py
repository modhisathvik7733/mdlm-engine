"""Bundled model loaders that wire fast_dllm-style extensions into otherwise
stock HF modeling code. Used to engage PATH A on models whose upstream
modeling lacks ``dual_cache`` / ``replace_position``.
"""
